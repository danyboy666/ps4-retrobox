# Large binary files — tracked via Git LFS
# community-files/arch.tar.xz, initramfs.cpio.gz, bzImage*, payload-* are tracked by LFS

# Release artifacts — do NOT add to repo (regenerated during release)
*.zip
ps4-ubuntu-es.tar.xz
/arch.tar.xz

# Build artifacts
/rootfs/
/tmp/
/mnt/ps4root/

# OS files
.DS_Store
Thumbs.db

# Editor
*.swp
*~
.vscode/

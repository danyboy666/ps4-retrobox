# Credentials & Secrets — NEVER COMMITTED
.env
.env.local
.env.*.local
*.pem
*.key

# OpenCode & Agent Config
AGENTS.md
.opencode/
.opencode-workspace/
opencode-logs/
opencode.json

# Local AI Scratchpads & Logs
opencode_history.md
ai_scratchpad.md
.opencode-logs/

# Large binary files — tracked via Git LFS
# community-files/arch.tar.xz, initramfs.cpio.gz, bzImage*, payload-* are tracked by LFS

# Release artifacts — do NOT add to repo (regenerated during release)
*.zip
ps4-ubuntu-es.tar.xz
/arch.tar.xz

# Exception: homebrew ROMs are tracked
!es_configs import/ROMS/**/*.zip

# Build artifacts
/rootfs/
/tmp/
/mnt/ps4root/

# OS files
.DS_Store
Thumbs.db

# Editor
*.swp
*~
.vscode/
community-files/ps4-retrobox-logo.png
community-files/ps4-retrobox-logo.svg
community-files/backup old img/
gamemode-daemon_*.deb
*.tar.xz
*.zip
*.cpio.gz
community-files/bzImage

# Large binary files — tracked via Git LFS
# *.tar.xz *.cpio.gz *.bin *.elf *.zip are tracked by LFS

# Community binaries — tracked via LFS
# community-files/bzImage* community-files/initramfs* community-files/payload-* are tracked by LFS

# Build artifacts
/rootfs/
/tmp/
/mnt/ps4root/

# OS files
.DS_Store
Thumbs.db

# Editor
*.swp
*~
.vscode/
